<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ntua.hci.mynnequin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mynnequin">
        <activity android:name=".CartActivityJav"></activity>
        <activity android:name=".ProductActivityJav" />
        <activity android:name=".FavoritesActivityJav" />
        <activity
            android:name=".SearchViewJav"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ModelPreview" />
        <activity android:name=".CartActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>